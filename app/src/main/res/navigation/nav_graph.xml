<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/splash_fragment">

    <fragment
        android:id="@+id/splash_fragment"
        android:name="br.com.uniptcc.brasilqueimadas.fragment.start.splash.SplashFragment"
        android:label="@string/splash_label"
        tools:layout="@layout/fragment_splash">

        <action
            android:id="@+id/action_splash_to_main_menu_"
            app:destination="@id/main_menu_fragment" />
    </fragment>

    <fragment
        android:id="@+id/main_menu_fragment"
        android:name="br.com.uniptcc.brasilqueimadas.fragment.home.MainMenuFragment"
        android:label="@string/main_menu_label"
        tools:layout="@layout/fragment_main_menu" />
</navigation>